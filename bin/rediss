#!/usr/bin/env bash

CLI="cli"
SERVER="server"
MODE="$1"

if [ "$MODE" = "$SERVER" ]; then
    ./rediss-server
elif [ "$MODE" = "$CLI" ]; then
    ./rediss-cli
else
    echo "unknow mode '$MODE', aborting . . ."
fi
